# ============================================================================
# Nova Platform - Contract Deployment Configuration
# ============================================================================
# Copy this file to .env and fill in your values before deployment.
# Run: cp .env.example .env
#
# IMPORTANT: Use the format below WITHOUT the 'export' keyword for Makefile
# compatibility. The Makefile uses 'include .env' which requires VAR=value format.
# ============================================================================

# ============================================================================
# REQUIRED: Network & Wallet Configuration
# ============================================================================

# PRIVATE_KEY: The private key of the deployer account
# - This account will be the initial owner/admin of deployed contracts
# - Must have sufficient ETH/native token to pay for gas fees
# - SECURITY: Never commit your actual private key to version control!
# - NOTE: Do NOT use 'export' prefix - just VAR=value format
PRIVATE_KEY=<your_private_key>

# RPC_URL: The RPC endpoint URL for the target blockchain
# Examples:
#   - Local Anvil: http://localhost:8545
#   - Base Sepolia: https://sepolia.base.org
#   - Base Mainnet: https://mainnet.base.org
RPC_URL=https://sepolia.base.org

# ============================================================================
# LOCAL DEVELOPMENT (Uncomment for local testing with Anvil)
# ============================================================================
# Default Anvil test account - NEVER use in production!
# PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
# RPC_URL=http://localhost:8545


# ============================================================================
# OPTIONAL: App Registration (for `make register-app`)
# ============================================================================

# PROOF_JSON: Path to the ZK proof file for attestation verification
# - Generated by nitro-attest-cli after fetching attestation
PROOF_JSON=samples/proof.json

# APP_URL: Public URL where the app is accessible
# - The endpoint that will be registered on-chain
APP_URL=http://example.com

# TEE_ARCH: The TEE (Trusted Execution Environment) architecture type
# Supported values:
#   - nitro: AWS Nitro Enclaves
#   - sgx:   Intel SGX (if supported)
TEE_ARCH=nitro

# CONTRACT_ADDR: (Optional) Address of a contract implementing INovaApp
# - If provided, registerTEEWallet() will be called on this contract
# - Default: address(0) if not set
# CONTRACT_ADDR=0x...

# METADATA_URI: (Optional) URI to metadata JSON for the app
# - Can be relative or absolute URL
# - Default: empty string if not set
METADATA_URI=/metadata.json

# AGENT_ATTESTATION_URL: URL to fetch attestation (for `make fetch-attestation`)
# - Typically ${APP_URL}/attestation
AGENT_ATTESTATION_URL=${APP_URL}/attestation

# ============================================================================
# OPTIONAL: Contract Verification (for `make verify-all-basescan`)
# ============================================================================

# BASESCAN_API_KEY: API key for Basescan contract verification
# - Get one at: https://basescan.org/myapikey
# - Required only for Basescan verification (Blockscout doesn't need API key)
BASESCAN_API_KEY=<your_basescan_api_key>
